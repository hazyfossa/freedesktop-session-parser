[package]
name = "freedesktop-session-parser"
version = "0.1.0"
edition = "2024"

[dependencies]
envy = { git = "https://github.com/hazyfossa/envy.git", version = "0.1.0", optional = true }
paste = "1.0.15"
snafu = "0.9.0"

[features]
env = ["dep:envy"]
